/*global window, document, define */
'use strict';
(function(window, document, undefined) {
  define(
    ['jquery', 'backbone', 'underscore'],
    function($, Backbone, _) {
      var TwitterHitter = function(
      );

  });
}(window, document));


